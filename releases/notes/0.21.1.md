<!-- zh-Hans -->
## 修复
- 修复 VS Code Remote SSH 环境中，Claude Code / Codex 插件通过远端 SSH 启动时 hook 无法触发的问题，对应 issue `#232`。

## 说明
- Sparkle build 号提升到 `56`，版本号提升到 `0.21.1`。
- 本次基于最新 `main` 发布，是 `0.21.0` 远端 SSH 命令包装改动后的 hotfix。

## 关联 PR / Issue
- `#232` VS Code Remote SSH 连接时，Claude Code / Codex 插件无法触发 Ping Island hook
- `#216` Wrap remote SSH commands in /bin/sh to avoid shell interpretation issues

<!-- en -->
## Fixes
- Fixed hooks not firing for Claude Code / Codex plugin sessions launched through VS Code Remote SSH, matching issue `#232`.

## Notes
- The Sparkle build number is now `56`, and the app version is now `0.21.1`.
- This latest-`main` hotfix release follows the remote SSH command wrapping change from `0.21.0`.

## Related PRs / Issues
- `#232` VS Code Remote SSH connections do not trigger Ping Island hooks for Claude Code / Codex plugin sessions
- `#216` Wrap remote SSH commands in /bin/sh to avoid shell interpretation issues
